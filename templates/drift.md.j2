# {{ repo_name }} — Drift Report

> **Trust trail**
> Config files scanned: `{{ sources_scanned | join("`, `") }}`
> Docs checked: `{{ doc_sources | join("`, `") or "none found" }}`
> Generated: {{ generated_at }} · Commit: `{{ commit_hash }}`

{% if not drift_items %}
No drift detected between config files and existing docs.
{% else %}
**{{ drift_items | length }} conflict(s) found.** Lead with #1 — it is the most concrete onboarding break.

---
{% for item in drift_items %}
## {{ loop.index }}. {{ item.category | replace("_", " ") | title }}

**① Source files imply**
{{ item.source_implies }}

**② Current doc says**
{{ item.doc_says }}

**③ Why it matters**
{{ item.why_it_matters }}

**④ Proposed patch**
{{ item.proposed_patch }}

**⑤ Confidence:** {{ item.confidence | upper }}
{% if item.unchecked %}
**Unchecked assumptions:**
{% for u in item.unchecked %}
- {{ u }}
{% endfor %}
{% endif %}

**Reviewer action:** {{ item.reviewer_actions | join(" · ") }}

---
{% endfor %}
{% endif %}

*Re-run with `--drift` after config or doc changes to catch new gaps.*
