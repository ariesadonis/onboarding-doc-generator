# {{ repo_name }} — Onboarding Guide

> **Trust trail** · Scanned: `{{ sources_scanned | join("`, `") }}`
> Generated: {{ generated_at }} · Commit: `{{ commit_hash }}`
> Reviewer: ________________ · Verified: ________________
> Unchecked assumptions: {{ unchecked | join(", ") if unchecked else "none flagged" }}

{% if readme_summary %}
> {{ readme_summary }}
{% endif %}

## Prerequisites

{% if languages %}
- Detected language(s): {{ languages | join(", ") }}
{% endif %}
{% if env_setup.get(".nvmrc") %}
- Node version: {{ env_setup[".nvmrc"] }}
{% endif %}
{% if env_setup.get(".python-version") %}
- Python version: {{ env_setup[".python-version"] }}
{% endif %}
{% if env_setup.get(".tool-versions") %}
- Tool versions (asdf): {{ env_setup[".tool-versions"] }}
{% endif %}
{% if env_setup.get("dockerfile") %}
- Docker required (Dockerfile present)
{% endif %}
{% if not languages and not env_setup %}
- No version/runtime markers detected — check the README manually.
{% endif %}

## Environment Setup

{% if env_setup.get("env_vars") %}
Copy `.env.example` to `.env` and fill in the following variables:
{% for var in env_setup["env_vars"] %}
- `{{ var }}`
{% endfor %}
{% else %}
No `.env.example` found — check the README or ask a teammate for required environment variables.
{% endif %}

{% if env_setup.get("docker_compose_services") %}
### Docker Compose Services
{% for service in env_setup["docker_compose_services"] %}
- {{ service }}
{% endfor %}

Run with: `docker compose up`
{% endif %}

## Install Dependencies

{% for ecosystem, info in dependencies.items() %}
### {{ ecosystem | replace("_", " ") | title }}
```
{{ info["install_command"] }}
```
{% if info.get("dependencies") %}
Key dependencies: {{ info["dependencies"][:10] | join(", ") }}{% if info["dependencies"] | length > 10 %}, ...{% endif %}
{% endif %}
{% endfor %}
{% if not dependencies %}
No recognized dependency manifest found (package.json, requirements.txt, Gemfile, etc).
{% endif %}

## Running the Project

{% if start_command %}
```
{{ start_command }}
```
{% else %}
No start command auto-detected — check README or package.json scripts.
{% endif %}

## Known Gaps

This doc was auto-generated from repo structure and may miss tribal knowledge
(infra secrets, internal services, non-standard setup steps). Please fill in
manually:

- [ ] Access requests (VPN, internal tools, credentials)
- [ ] Team-specific conventions
- [ ] Common pitfalls / "gotchas"

---
*Generated by onboarding-doc-generator from `{{ repo_name }}` @ commit `{{ commit_hash }}` on {{ generated_at }}.*
*Re-run the generator after major dependency or setup changes to keep this current.*
